/**
 * Core types — source unique depuis le package partagé.
 * Aucune définition locale pour éviter la duplication.
 *
 * @module core/types
 */

export type {
  AddressFeature,
  Coordinates,
  DvfIndicators,
  RiskCategory,
  RiskDocument,
  RiskScoreResult,
  RiskZonesGeoJSON,
  ScoreInterpretation,
} from '@risquesavantachat/shared-types';
